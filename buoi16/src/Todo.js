import React from 'react';

const Todo = (props) => {
    return (
        <div>
            <input type='radio' />
            <label>Clean up bedroom</label>
        </div>
    );
};

export default Todo;
